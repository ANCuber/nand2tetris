class Main {
    function void main() {
        var CircleGame game;
        let game = CircleGame.new(50, 60, 4, 1, 1, 3, -2, 50);
        // x, y, size, ax, ay, vx, vy, angle
        do game.run();
        do game.dispose();
        return;
        
    }
}