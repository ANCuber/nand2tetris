class Main {
    function void main() {
        var CircleGame game;
        let game = CircleGame.new(256, 20, 10, 0, 0, 0, 0, 90);
        do game.run();
        do game.dispose();
        return;
        
    }
}