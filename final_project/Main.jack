class Main {
    function void main() {
        // Variables and Objects
        var Kid kd;
        var Ground gnd;

        // Clear the screen
        do Screen.clearScreen(); // note that the screen is 256*512
        
        let kd = Kid.new(100, 100, 8);
        let gnd = Ground.new(200, 200);

        do Sys.wait(100);

        do gnd.moveUp(20);
        
        return;
    }
}

